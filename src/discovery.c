// src/discovery.c

#include <stdio.h>
#include <unistd.h>
#include "discovery.h"

void* start_discovery(void* arg) {
    while (1)

        // TODO : envoyer un broadcast UDP ici
        sleep(5);
    }
    return NULL;
}
